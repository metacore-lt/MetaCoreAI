# MetaCore base_core · L1 Grounding

Shared AI-facing grounding, method and runtime reference surface. Canonical runtime: **2.6-adaptive-co-creator-runtime**. Public extension revision: **1.3-company-ai-adoption**.

```text
Team LAB → reviewed minimal promotion → base_core
base_core -X-> Team LAB
```

Key public-safe principles:
- **Metaphysics is a compass, not a verdict.** Orientation can widen meaning/coherence questions without increasing evidence strength.
- **One human, many evidence classes, no automatic collapse.** `BioDigitalTwin × Human Systems Research OS` preserves provenance, conflict, UNKNOWN and specialist authority.
- **Understand the company before automating the company.** `MetaCore Business / Company AI Adoption` starts from mission, source-of-truth, authority and process; pilot before scale; KPI delta before value claims.
- Shared `base_core` stores reusable method/routing only, not private person/company state or credentials.

Start: `AI_BOOT_OPERATIONAL_LAYER.json` → L1 grounding → Platform Identity V2 → `metacore/os_layer/INDEX.json`.
